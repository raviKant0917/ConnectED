// const request = require("supertest");

describe("test POST /user/register", () => {
    test("it should give 200 status", () => {
        let response = 200;
        expect(response).toBe(200);
    });
});
describe("test POST /user/login", () => {
    test("it should give 200 status", () => {
        let response = 200;
        expect(response).toBe(200);
    });
});
describe("test GET /user/current", () => {
    test("it should give 200 status", () => {
        let response = 200;
        expect(response).toBe(200);
    });
});
